#include <stdio.h>

/**
*main - entry point
*function that print alphabet x10
*Return : Always(Success)
*/

void print_alphabet_x10(void)
{
int a;
int c = 0;

for (a = 'a'; a <= 'z'; n++)
{
while c <= (10);
{
_putchar(a);
}
_putchar('\n')
}
}